jquery选择器
用处：
	爬虫：抓取数据
	减少网页权重，少使用id选择器 防止爬虫抓取数据
七大选择器：
	层次选择器
	过滤选择器
		内容过滤
		基本过滤
		可见性过滤
		表单过滤
		属性过滤
		子元素过滤
层次选择器
	body>p表示只是body下面的p标签，>高级选择器有效
	+ ~
	+表示邻近的同级元素，后面不能有别的标签存在
	~表示全部的同级别元素
	考虑层次选择器的原则向后，前面元素不理
过滤选择器
	1、基本过滤
		first 第一个元素,没有级别限制
			用法：$("p:first")
		last最后一个元素
		even偶
		odd奇
		应用：（1）表格奇偶行颜色：索引从0开始
		等于某行：eq(0)第一行
		大于某行：gt(0)
		小于某行：lt(0)
	内容过滤过滤
		contains内容包	含 contains(‘标签中间的内容‘）	
		empty()
	可见性选择器：
		分步表单（第一数据填写完，进入到第二页，第一步没有提交到服务器，在第二页有一个input框，这个框是hidden的），这个hidden框叫（埋点）
	visible所有显示的元素
	hidden所有隐藏的元素
	子元素过滤选择器
		first-child
		last-child
		nth-child第几个(表达式）	
	表单选择器
	jquery封装了一个each方法,对循环的元素做遍历
		$(":checked").each(function(index,item){
				console.log(index,item)
			})
		两个参数，第一个参数是脚标，第二个参数是元素
	:checked所有被选中的元素
	属性选择器
	input[属性=属性值]
	不等 ！=
	开头等于某值 ^=
	结尾等于某值 $=
	出现任意地方 *=
